# Environment Configuration for TEC Platform
# Copy this file to .env and fill in your values

# ===== REQUIRED SECRETS =====
# These MUST be set for the platform to run

# PostgreSQL Database Password (strong password recommended)
POSTGRES_PASSWORD=

# JWT Secrets (minimum 32 characters, use strong random strings)
JWT_SECRET=
JWT_REFRESH_SECRET=

# ===== OPTIONAL CONFIGURATION =====

# Environment
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=*

# JWT Configuration
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging (error, warn, info, debug)
LOG_LEVEL=info

# Pi Network (required for Pi payment integration)
# Obtain from https://developers.minepi.com
PI_API_KEY=
PI_APP_ID=

# Pi Network environment: 'true' for Testnet/Sandbox, 'false' for Mainnet/Production
PI_SANDBOX=true

# Pi API request timeouts in milliseconds (default: 30000ms = 30 seconds)
# PI_API_APPROVE_TIMEOUT=30000
# PI_API_COMPLETE_TIMEOUT=30000

# ===== FRONTEND (NEXT_PUBLIC_*) VARIABLES =====
# These are exposed to the browser and set at build time

# Client-side Pi App ID
NEXT_PUBLIC_PI_APP_ID=

# Client-side Pi sandbox mode
NEXT_PUBLIC_PI_SANDBOX=true

# ===== INSTRUCTIONS =====
# 1. Copy this file: cp .env.example .env
# 2. Generate strong secrets:
#    - For JWT_SECRET and JWT_REFRESH_SECRET, use:
#      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    - For POSTGRES_PASSWORD, use a strong password manager
# 3. Fill in all REQUIRED values in .env
# 4. Run: docker-compose up -d
